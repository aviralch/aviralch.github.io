import React from 'react';

function NavBarItems(props) {
  return (
    <div className="nav-bar-items" style={{ left: props.marginLeft}}>
      {/* {props.pageTitle} */}
    </div>
  );
}

export default NavBarItems;